import DonutIndex from '../../../app/javascript/react/containers/DonutIndex'
import DonutTile from '../../../app/javascript/react/components/DonutTile'

describe('DonutIndex', () => {
  let wrapper;

  beforeEach(() => {
    jasmineEnzyme();
    wrapper = mount(<DonutIndex />);
  });


})
 
